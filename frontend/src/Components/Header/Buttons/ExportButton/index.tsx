import { GenericButton } from "..";
import { ExportIcon } from "../../../../assests/svgIcons";

export default function ExportButton() {
  return (
    <GenericButton
      icon={
        ExportIcon
      }
      text="Export"
      onClick={() => {}}
    />
  );
}
